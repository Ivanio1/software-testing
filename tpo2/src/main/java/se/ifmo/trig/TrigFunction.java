package se.ifmo.trig;

import se.ifmo.AbstractFunction;

public abstract class TrigFunction extends AbstractFunction {
    @Override
    public Double checkX(double x) {
        return super.checkX(x);
    }
}